%h1
  Courses:

.card-columns
  - @courses.each do |course|
    = render 'courses/course', course: course

%table.table
  %thead
    %tr
      %th Title
      %th Description
      %th User
      %th
      %th

  %tbody
    - @courses.each do |course|
      %tr
        %td= course.title
        %td= course.description
        %td= course.user.email
%br

